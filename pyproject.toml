[project]
name = "budget-core"
version = "0.1.0"
description = "Budget (M.B) historical migration core"
requires-python = ">=3.11"

[project.scripts]
budget = "budget.cli:main"

[tool.unittest]
start-directory = "tests"

